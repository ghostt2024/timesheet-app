## 🔐 First-Time Setup
1. Copy `.env.example` to `.env` and fill in secrets
2. Run `npm install`
3. Run `node setup-admin.js` to create first admin
4. Start server: `node server.js` or `.\start-app.bat`